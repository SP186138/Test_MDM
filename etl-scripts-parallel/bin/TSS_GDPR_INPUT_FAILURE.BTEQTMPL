.RUN FILE $LOGON_FILE;
.SET ERROROUT STDOUT;
.SET SESSION CHARSET 'UTF8';

UPDATE LOAD_INFO
SET STATUS = 'Failure'
,PROCESS_END_TIME = CAST(CURRENT_TIMESTAMP(0) AS VARCHAR(19))
WHERE MDM.LOAD_INFO.PROCESS_NAME='GDPR Input'
AND MDM.LOAD_INFO.STATUS = 'In Progress'
;

.IF ERRORLEVEL > 0 THEN .QUIT ERRORCODE;

.QUIT 0
